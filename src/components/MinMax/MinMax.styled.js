import styled from "styled-components";

export const MinMaxTitle = styled.h2`
  display: flex;
  justify-content: space-between;
  margin-top: 2rem;
`;

export const MinMaxValues = styled.div`
  display: flex;
  justify-content: space-between;
  padding: 1rem;
  margin-top: 1rem;

  border: 2px solid #ffffff;
  border-radius: 20px;
  box-shadow: var(--shadow);
`;
